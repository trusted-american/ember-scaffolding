import Component from '@glimmer/component';

/* 
 * ARGS
 * 
 * centered?: boolean
 * large?: boolean
 * color?: 'primary'|'secondary'|... = 'secondary'
 * 
 */

export default class UiSpinnerComponent extends Component {
}
