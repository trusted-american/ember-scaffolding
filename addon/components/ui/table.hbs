<YetiTable @data={{@data}} @loadData={{@onLoad}} @pagination={{@pagination}} @pageSize={{@pageSize}} @pageNumber={{@pageNumber}} @sortable={{@sortable}} ...attributes as |table|>
	{{#if table.isLoading}}
		<Ui::Spinner @centered={{true}} />
	{{else}}
	{{/if}}
	{{yield table}}
	<table.tfoot as |foot|>
		<foot.row as |row|>
			<row.cell @visible={{true}} colspan={{table.columns.length}}>
				<table.pagination />
			</row.cell>
		</foot.row>
	</table.tfoot>
</YetiTable>
